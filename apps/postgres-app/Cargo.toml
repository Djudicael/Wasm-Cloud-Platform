[package]
name = "postgres-app"
version = "0.1.0"
edition = "2021"

[dependencies]
url = { workspace = true }

[target.'cfg(not(target_family = "wasm"))'.dependencies]
axum = { workspace = true }
tokio = { workspace = true }
